/**
 * Settings Component
 * 
 * This component provides settings menu with update checking and configuration options.
 * The business logic is handled in SettingsContainer, while the UI rendering is in SettingsPresenter.
 */

import { SettingsContainer } from '@renderer/features/layout/components'

export function Settings() {
  return <SettingsContainer />
}
