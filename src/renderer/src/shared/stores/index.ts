/**
 * Shared Stores Index
 * 
 * Global application state stores that are used across multiple features.
 */

export { useThemeStore } from './useThemeStore'
export type { ThemeInfo, ThemeMode } from './useThemeStore'
